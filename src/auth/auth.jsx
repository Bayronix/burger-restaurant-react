import styles from "./auth.module.css";

const Auth = () => {
  return (
    <div>
      <button type="submit">Sign In</button>
      <button type="submit">Login</button>
    </div>
  );
};
export default Auth;
